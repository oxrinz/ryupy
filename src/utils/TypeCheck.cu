          //  if (typeid(*this) != typeid(other))
          //  {
          //      std::cerr << "Error: Incompatible tensor types for addition." << std::endl;
          //  }
          //  return add_impl(other);